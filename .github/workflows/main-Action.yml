name: main-Action

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Start build and lint project"
      - run: cd app/
      - run: make lint

