name: main Action

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        uses: actions/checkout@v4
      - name: build and test project
      - run: cd app/
      - run: make lint

